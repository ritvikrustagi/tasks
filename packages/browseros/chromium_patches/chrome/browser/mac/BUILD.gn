diff --git a/chrome/browser/mac/BUILD.gn b/chrome/browser/mac/BUILD.gn
index 14a8b883a382ffa08bfc0948003b0b12cde693c4..45aa308a70d5d49424d0f2070a8b2ba6ea84a0a1 100644
--- a/chrome/browser/mac/BUILD.gn
+++ b/chrome/browser/mac/BUILD.gn
@@ -49,6 +49,13 @@ source_set("key_window_notifier") {
   public_deps = [ "//base" ]
 }
 
+# Keep the Sparkle interface independently reachable by updater UI targets.
+# The implementation remains in //chrome/browser:core because it uses browser
+# process state, while :core already depends on the updater UI.
+source_set("sparkle_glue_headers") {
+  public = [ "sparkle_glue.h" ]
+}
+
 source_set("mac") {
   public = [
     "auth_session_request.h",
